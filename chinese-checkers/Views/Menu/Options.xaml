<Page
    x:Class="chinese_checkers.Views.Menu.Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:chinese_checkers.Views.Menu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:chinese_checkers.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush ImageSource="ms-appx:///Assets/Images/splash.png"></ImageBrush>
    </Page.Background>

    <Viewbox>
        <Grid>
            <StackPanel>
                <TextBlock>Options</TextBlock>
                <Slider x:Name="speedSlider" Header="Speed" Width="200" ValueChanged="speedSlider_ValueChanged" Value="{x:Bind test, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" DataContext="Animation" TickFrequency="2" Minimum="1" Maximum="20" SnapsTo="Ticks"/>
                <Button x:Name="backButton" Click="backButton_Click" FontSize="20" HorizontalAlignment="Center">Back</Button>
            </StackPanel>
        </Grid>
    </Viewbox>
</Page>
